image: continuumio/anaconda3

before_script:
  - apt-get --quiet update --yes
  - apt-get --quiet install --yes git
  - pwd
  - ls -la
  - echo $UID
  - chmod +x ./base_script.sh
  - ls -la

stages:
  - build

build:
  stage: build
  script:
    - /bin/bash ./base_script.sh
